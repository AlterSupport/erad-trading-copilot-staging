# -----------------------------------------------------------------------------
# Environment variables for the Erad Trading Copilot Frontend
#
# After running the `cloud-function/deploy.sh` script, it will output the
# URLs for your deployed cloud functions. Copy and paste them here.
# -----------------------------------------------------------------------------

# The URL for the blotter upload cloud function.
# The frontend API route (/api/upload-blotter) will call this endpoint.
CLOUD_FUNCTION_URL="YOUR_UPLOAD_BLOTTER_CLOUD_FUNCTION_URL"

# The URL for the Natural Language Understanding (NLU) cloud function.
# This is used by the chat interface to process user messages.
NEXT_PUBLIC_NLU_FUNCTION_URL="YOUR_NLU_SERVICE_CLOUD_FUNCTION_URL"
